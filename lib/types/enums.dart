enum PrimerButtonSize { small, large, medium }

enum PrimerButtonVariant { primary, danger, outline, invisible }

enum PrimerTooltipDirection { n, ne, e, se, s, sw, w, nw }

enum PrimerLabelSize { small, large }

enum PrimerLabelVariant {
  primary,
  secondary,
  accent,
  success,
  attention,
  severe,
  danger,
  done,
  sponsors
}
